#include <stdio.h>

int main()
{
    int guj = 46;
    int maths = 42;
    int eng = 32;
    int hindi = 34;
    int com = 40 ;
    int total;

    // float percentage , total;

    // scanf("%f%f%f%f%f", &Guj, &maths, &Eng, &Hindi, &com);

    // total subject calculate

    // total = guj + maths + eng + hindi + com;

    printf("total = %d" , guj);

    // printf("percentage = %.2f%%", (total / 500) * 100);

    // printf("percentage = %.2f%%", (total / 500) * 100);
    return 0;
}